{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd dashboard && npm install && npm run build && cd .. && npx tsx bot-with-dashboard.ts",
    "restartPolicyType": "always",
    "restartPolicyMaxRetries": 10
  }
}
